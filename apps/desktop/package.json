{
  "name": "@yksprite/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist-electron/main/index.js",
  "scripts": {
    "lint": "tsc --noEmit -p tsconfig.json",
    "build": "vite build --config electron.vite.config.ts",
    "package": "pnpm run build && electron-builder --config electron-builder.yml"
  },
  "files": [
    "dist-electron",
    "electron-builder.yml"
  ],
  "devDependencies": {
    "@types/electron": "^1.6.12",
    "@types/node": "^22.5.1",
    "electron": "^27.3.0",
    "electron-builder": "^24.8.0",
    "electron-vite": "^5.0.0",
    "tslib": "^2.7.1",
    "typescript": "^5.8.3",
    "vite": "^5.2.0"
  }
}
